# 理解度チェック回答：Webアプリケーション開発 ITリテラシー理解度チェック

作成日: 2026-08-07
対象: タスク管理アプリ「github-demo」の開発を通じて身につけた内容の理解度チェック（全32問、4章構成）。AIと壁打ちしながら、自分の言葉でまとめたもの。

**進捗: 32/32問 回答済み（Q1〜Q32）。全問完了。**

---

## 第1章 インターネットとネットワークの仕組み（12問）

### Q1. インターネットとは

「インターネット」と「Webブラウザで見るホームページ」は同じものですか？違うとすれば、どう違いますか？

**回答:**
違います。インターネットは世界中のコンピューター同士を繋ぐ通信網そのものです。
Webブラウザで見るホームページは、インターネットという通信網の上で動くサービスの一つです。他には、メール、動画配信、オンラインゲームなど、インターネット上には様々なサービスがあり、Webブラウザはその中でのある一つのサービスに過ぎません。

### Q2. IPアドレスとポート番号

このプロジェクトではフロントエンドを`localhost:5173`、バックエンドを`localhost:8080`で起動している。`localhost`とは何を指すか、ポート番号はなぜ必要か。

**回答:**

- localhostは、自分自身のコンピューターを指す特別な名前です。（IPアドレスでいうところの127.0.0.1です）
今回のカリキュラムで進めているタスク管理ツールのフロントエンドとバックエンドは同じPC上で動かしているのでlocalhostでお互い呼び合っています。
- ポート番号は、同じコンピューターの中でどのアプリ宛ての通信かを区別するための番号の事です。（例えばマンションのように一つの建物の中に複数の部屋番号があるイメージです）
5173は、Vite（フロントエンド）の番号、8080は、Spring Boot（バックエンド）の番号で、同じPCの中に別々にあるので、番号で区別する必要があります。
※これは実際にCLAUDE.mdにもポート固定のルールとして明記している内容です。



### Q3. DNS

ブラウザのアドレスバーに`https://www.example.com`と入力してからページが表示されるまでに、DNSはどのような役割を果たすか。

**回答:**
DNSは人間が読みやすいドメイン名(www.example.com)をコンピューターが通信で使うIPアドレスに変換する仕組みの事。電話帳のような役割を果たしている。

### Q4. HTTPとは

HTTPとは何の略で、どのような役割を持つか。「プロトコル」という言葉の意味も含めて。

**回答:**
HTTPとは(HyperText Transfer Protocol)の略称の事。
Webブラウザ(クライアント)とWebサーバーの間で、Webページや画像などのデータを送受信するための約束事のようなものです。
「プロトコル」は、通信を行うための共通の約束事(ルール・手順)の事です。
言葉や形式のルールを決めておくことで、異なるメーカーやOSの機器同士でも正しく通信ができるようになる。

### Q5. HTTPとHTTPSの違い

HTTPとHTTPSの違いは何か。なぜ現在のWebサイトはほぼすべてHTTPSを使用しているか。

**回答:**
HTTPとはデータを暗号化せずに生データをそのまま送受信する仕組みの事。
HTTPSは、HTTPにSSL/TLSという暗号化技術を組み合わせて、データを暗号化して安全に送受信する仕組みの事です。
なので、HTTPは何もせずにHTTPSは安全対策した状態の事で、「S」は、Secureの意味で安全なという意味。

1. 盗聴や改ざんの防止：個人情報、パスワード、クレジットカード情報などが第三者に盗み見られたり悪用されるリスクを防ぐためです。
2. 成りすましの防止：通信相手が本物のWebサイトであることを証明し、偽サイト(フィッシング詐欺)を防ぐためのものです。
3. 検索順位や警告への対策：Googleなどの検索エンジンが安全性を重視しており、HTTPのままだとブラウザで保護されていない通信と警告表示が出てユーザーが離脱してしまう恐れがあるためです。



### Q6. リクエストとレスポンス

Webにおける「リクエスト」と「レスポンス」の関係を、日常生活の例えを使って説明。

**回答:**
日常で例えると、例題として(レストランでの注文)
リクエスト(要求)：お客さんが店員さんに「ハンバーグセットをください」と注文票(オーダー)を渡すこと。※Webブラウザからサーバーへ「このデータをください」と送信することと一緒です。
レスポンス(応答)：厨房(サーバー)で料理を作り、店員さんが「お待たせしました！」と出来上がったハンバーグセットを運んできてくれること。※サーバーからブラウザへWebページデータや処理結果を返信することと一緒です。

### Q7. HTTPメソッド

このアプリのAPIではGET/POST/PUT/DELETE/PATCHを使っている。これらの「HTTPメソッド」とは何か、日常生活の例えでそれぞれの違いを説明。

**回答:**
・HTTPメソッドとは、リクエストを送る際にサーバーに対して「どのような操作(取得・登録・更新・削除など）をしてほしいか」を伝える命令(アクション)の種類のことです。
・日常生活での例え「レストラン」
GET(取得)：「メニューを見せてください」「料理を持ってきてください」
役割として：データを読み取って取得する(見るだけ)。
POST(新規作成)：「新しい料理を追加で注文します」
役割として：サーバーに新しいデータを保存・作成をすること。
PUT(全体更新・置換)：「注文を全面的に別のセットメニューに変更してください」
役割として：既存データを丸ごと新しい内容に置き換えることです。
DELETE(削除)：「さっきの注文をキャンセル(削除)してください」
役割として：データを削除することです。
PATCH(部分更新)：「ハンバーグのソースだけデミグラスから和風に変更してください」
役割として：既存データの一部だけをピンポイントで修正・更新することです。
※ただしこのアプリでは実際にはPATCHは使われておらず、更新は全てPUTで行っている。

### Q8. HTTPステータスコード

200・201・400・404・500はそれぞれ何を意味するか。

**回答:**
200(成功):データの取得に成功したなど。
201(新規作成成功)：POSTリクエストなどで、新しいタスクやユーザー情報がDBに正しく登録されたことを示します。
400(リクエスト内容がエラー)：必須項目などが抜けている、データの形式が違うなど、リクエストを送った側に問題があることを示します。
404(対象が見つからない)：URLの入力違いや既に削除されたデータへアクセスしようとしたことを示します。
500(サーバー内部エラー)：バックエンドのプログラムのバグやDB接続の不具合など、サーバー側に問題があることを示す。

このアプリの`CardController`でも、存在しない`columnId`を指定して更新しようとすると、本来は400を返すべきところが、修正前は例外がそのまま起きて500になっていた。PR #20で「事前にIDの存在チェックをしてBAD_REQUEST(400)を返す」よう直したことで、クライアント側のミスとサーバー側の不具合を正しく区別できるようになった。

### Q9. JSONとは

JSONとは何か、なぜ広く使われているか。

**回答:**
・JSONとは、JavaScript Object Notationの略称で、データをテキスト(文字)形式で表現するフォーマットの事です。
{"name"："タスク名","status":"完了"}のように、「キー(項目名)」と「値(内容)」をペアに記述するシンプルな構造を持っていることです。

1. 人間が見ても直感的に理解しやすい：シンプルなテキスト形式なので、エンジニアが読み書きやデバッグ(エラー確認)をしやすいためです。
2. どんなプログラミング言語でも扱える：JavaScriptだけでなく、Java、Python、PHPなど、ほぼすべてのプログラミング言語に対応していて簡単に読み書きができる共通フォーマットです。
3. データ量が軽くて高速：構造がシンプルなのでデータサイズも小さく済み、フロントエンドとバックエンド間の通信速度を早く保つことができる。



### Q10. APIとは

APIとは何か、なぜフロントエンドは直接データベースにアクセスしないのか。

**回答:**
・APIとは、Application Programming Interfaceの略称で、ソフトウェア同士がデータや機能をやり取りするための接続窓口(インターフェース)の事です。
フロントエンドからの「タスク一覧をください」「新しいタスクを保存してください」といったリクエストを受け取り、バックエンドで処理をして結果を返す役割を持っている。

1. セキュリティ(安全対策)：フロントエンド(ブラウザ側)からDBに直接アクセスできるようにすると、DBのパスワードなどの機密情報が外部に漏れ、悪意あるユーザーにデータを直接改ざん・削除される危険があるためです。
2. ビジネスロジックの検証：「必須項目が入っているか」「権限があるユーザーか」などのルール確認・チェック処理をバックエンド側で挟んでやるためです。
3. 役割分担と保守性：画面の見た目(フロントエンド)とデータの保持(DB)を分離することで、コードの変更や管理がしやすくなるためです。



### Q11. CORSとは

フロントエンド（5173）からバックエンド（8080）にアクセスする際に関係する「CORS」とは何のためにあるか。

**回答:**
・CORSとは、Cross-Origin Resource Sharing(クロスオリジンリソース共有)の略称です。

1. 悪意あるWebサイトからの不正なデータアクセスを防ぎ、ブラウザ通信のセキュリティを保護するためにあります。
2. ブラウザには「異なるオリジン(ポート番号やドメインが違う場所。例5173と8080)」同士の通信を基本的にブロックするセキュリティルールがあります。
3. CORSは、バックエンド側で「ポート番号5173からのリクエストなら許可するよ」とあらかじめ設定しておくと信頼できる特定のフロントエンドからのアクセスだけを安全に許可・通信できるようにする仕組みの事です。



### Q12. クライアントとサーバー

「クライアント」と「サーバー」とは何か。このアプリの構成（ブラウザ／バックエンド／データベース）の中でそれぞれどれがクライアントでどれがサーバーか。

**回答:**
・「クライアント」：サービスを利用する側(「〇〇のデータをください」と要求・リクエストを送る側）の事です。
「サーバー」：サービスを提供する側(クライアントからの要求を受け取り、処理やデータ提供などのレスポンスを返す側)の事です。
・このアプリでは、クライアントがブラウザ(ユーザーの画面操作からのリクエストを送信するため)
サーバーはバックエンド・DB(リクエストを受け取り、データの計算処理や保存・取得をするため)
ただし、バックエンドは常に「サーバー」というわけではなく、DBに対してリクエストを送る場面ではバックエンド自身が「クライアント」、DBが「サーバー」になります。つまりクライアント/サーバーは固定の役割ではなく、誰から見た関係かによって入れ替わる相対的なものです。

---



## 第2章 Webアプリケーションの構造（8問）

> 注記: Q16の設問は「`/api/tasks`」という前提で書かれているが、このプロジェクトの実際のエンドポイントは`/api/cards`・`/api/columns`。回答時は自分のプロジェクトのURLに読み替えて書く。



### Q13. フロントエンドとバックエンド

「フロントエンド」と「バックエンド」の違いを説明してください。
このアプリではそれぞれ何を担当していますか？

**回答:**
フロントエンド：ユーザーが直接見て操作する画面(見た目/UI)側の事です。
バックエンド：ユーザーからは見えない裏側の仕組み(ビジネスロジック・処理)を担当する側の事です。
・フロントエンド(React/HTML/CSSなど)：タスク一覧の表示、入力フォーム、ボタンなどの見た目の作成やユーザーのボタン操作を受けてAPIにリクエストを送信する処理を担当している。
・バックエンド(Java/Spring Bootなど)：フロントエンドから送られてきたデータの内容をチェック、計算処理、DBへの保存・読み出し処理・結果をJSON形式で返信する処理を担当している。

### Q14. 3層構造

このアプリは

- フロントエンド
- バックエンド
- データベース

の3層構造です。なぜこのように役割を分けるのですか？1つのプログラムですべてをやらない理由を説明してください。

**回答:**

1. 保守性(メンテナンス性)の向上：画面のデザインを変更したいとき、DBの種類を変更したいときにその他の部分に影響を与えずに該当する部分だけを修正・変更ができるようにするため。
2. 開発効率と分業のしやすさ：画面を作るエンジニアと裏側のロジックを作るエンジニアで作業を完全に分担して、並行して開発を進められるようにするためです。
3. セキュリティの強化：重要なDBやビジネスロジックを直接ユーザー(ブラウザ)から見えない位置に配置し、backエンドを挟むことで不正アクセスや改ざんを防ぐためです。
4. 再利用性と拡張性：将来的にスマホアプリ版(iOS/Android)を作りたいときにでも、バックエンドとDB(3層構造の裏側)をそのまま再利用して画面だけを増設することもできるようになるためです。



### Q15. データの流れ

ユーザーが「新しいタスクを作成」ボタンを押してから、

- データベースに保存される
- 画面に反映される

までの流れを順を追って説明してください。

**回答:**

1. フロントエンド(ブラウザ)でのイベント発生とリクエスト送信

ユーザーが入力フォームにタスク内容を入力して「新しいタスクを作成」ボタンを押すと、フロントエンド側でイベントが発生し、入力データをJSON形式に変換してバックエンドのAPI(POST /api/tasks)へHTTPリクエストを送信する。

1. バックエンドでの受信と処理・DB保存

バックエンド(Java/Spring Bootなど)がリクエストを受信して、データの妥当性チェックを行い、そのあとDBへ保存するための処理を実行する。SQL(INSERT文)を発行して、DBに新しいタスクレコードを保存する。

1. バックエンドからのレスポンス返却

DBへの保存が成功したら、バックエンドは保存された新しいタスクデータをJSON形式で包み、HTTPステータスコード(201 Created)と一緒にフロントエンドへレスポンス(返却)をします。

1. フロントエンドでの画面更新(反映)

フロントエンドが成功のレスポンスを受け取り、返却された新しいタスクデータを既存のタスク一覧のデータに追加して画面を再描画(レンダリング)をする。これにより、ユーザーの画面に新しいタスクが即座に反映されることになります。

### Q16. エンドポイントとURL設計

このアプリには以下のAPIがあります。

- `/api/tasks`
- `/api/tasks/{id}`

このようなURL設計の考え方を説明してください。`{id}` の部分は何ですか？

**回答:**
エンドポイントはAPIの窓口URL。
・URL設計の考え方：リソース(操作対象のデータ)を名詞で表す。URLには操作内容(getやcreateなど)を含めず、`/api/tasks`のように操作したいデータそのもの(リソース)を名詞の複数形(tasks)でシンプルに表現をする。
HTTPメソッドと組み合わせる：どのような操作(取得・登録・更新・削除)をするかはURLではなくHTTPメソッド(GET,POST,PUT,DELETE)で使い分けることになります。
例：GET /api/tasksは、タスク一覧を取得
例：POST /api/tasksは、タスクを新規作成

・`{id}`の部分とは：パスパラメータ(特定データを識別するための識別子です)。
DB内で個々のタスクを一意に識別するためのID番号(例：1、2、3…)がここに入る。
`/api/tasks/5`のように指定することで、IDが5番の特定のタスクに対してのみ個別で操作(取得・登録・更新・削除)を行うことができます。

### Q17. 同期処理と非同期処理

「同期処理」と「非同期処理」の違いを、日常生活の例えで説明してください。なぜWeb通信では非同期処理が必要ですか？

**回答:**
・同期処理：一つの作業が終わるまで、次の作業を行わずにその場で待ち続ける処理の事です。
例：レジで注文した商品が完成するまで、後ろに並んでいる人も含めて全員がそのままじっと立ち止まって待っている状態。

・非同期処理：作業(リクエスト)を頼んだ後、結果を待たずに別の作業を並行して進める処理の事です。
例：レジで注文して「呼び出しベル(引換券)」を受け取り、料理が完成するまでの間に席を探したり、スマホを観たりして過ごし、ベルが鳴ったら料理を受け取りに行く状態。

・Web通信で非同期処理が必要な理由
画面のフリーズ(操作不能)を防ぐため：サーバーとの通信(データの取得や保存)には時間がかかることもある。
もし同期処理で通信を行うと、データが返ってくるまで画面全体の操作が完全に固まってしまいます。非同期処理通信を使うことで、通信中もユーザーが画面をスクロールしたり他のボタンを押したりできるようにし、快適な操作性(ユーザー体験)を提供できるためです。

### Q18. SPA（シングルページアプリケーション）

このアプリはReactで作られたSPAです。従来のWebサイト（ページ遷移するもの）と何が違いますか？

**回答:**
・従来のWebサイト：ページを移動したり更新したりするたびに、サーバーからHTML全体（画面全体）を毎回読み直して表示します。その為、画面遷移のたびに画面が一瞬白くなり、ページの切り替えに時間がかかります。

・SPA（シングルページアプリケーション）：最初に一回だけベースとなるHTMLを読み込み、ページ切り替えやデータ更新の際は必要な部分のデータ(JSONなど)だけをAPI経由で取得してJavaScriptで画面の一部だけを差し替えます。

・主なメリット：画面全体のリロードが発生しないため、スマホアプリのようにサクサクと滑らかに画面が動きます。

### Q19. 入力チェック（バリデーション）の多重防御

入力チェックはフロントエンドとバックエンドの両方で行うべきと言われています。なぜ片方だけでは不十分なのですか？

**回答:**

・フロントエンドだけでは不十分な理由
フロントエンド(ブラウザ側)のJavaScript処理や入力チェックは、悪意あるユーザーによって開発ツールなどで簡単に回避・無効化されたり、APIへの直接な不正なリクエストを送信されたりする可能性があります。その為、バックエンドでのチェックがないと不正なデータがそのままDBに保存されてしまう危険性があります。

・バックエンドだけでは不十分な理由
バックエンドだけでチェックを行うと、入力ミス(メールアドレスの形式が違う、必須項目が空欄など)があるたびに毎回サーバーへ通信を送ってエラー結果を受信する必要があり、ユーザーへのエラー表示が遅くなり不便を感じさせることになる。

・結論(多重防御が必要な理由)
フロントエンド側：ユーザーに「即座にミスを知らせて快適に操作をしてもらうため」(UXの向上)
バックエンド側：悪意あるアクセスや不正なデータからシステムとDBを確実に守るため(セキュリティの確保)
このように役割が異なるため、両方で二重チェックで行う多重防御が必要不可欠になります。

### Q20. エラーハンドリング

API呼び出しが失敗した場合（ネットワーク障害やサーバーエラーなど）、アプリはどのように対処すべきですか？

**回答:**

・アプリがとるべき主な対処

1. ユーザーへの明確なフィードバック(エラー表示)
画面が真っ白になったり無反応になったりするのを防ぎ、「データの取得に失敗しました。時間をおいて再度お試しください」といった分かりやすいメッセージやアラート画面を表示させるようにする。

2. 開発者向けのログ出力と原因追究
ブラウザのコンソールやサーバーログに詳細なエラー内容(エラーコードなど)を出力し、あとから原因調査・復旧ができるようにする。

3. 適切な状態の維持と再試行(リトライ)
不正なデータで画面が描画されるのを防ぎ、必要に応じて「再読み込みボタン」を配置したり、ネットワーク復旧時に自動でリクエストを再試行する仕組みを用意するようにする。

なお、このアプリでは`useCards.js`の各処理が`try/catch`で失敗を捕まえ、`setError(LOAD_ERROR_MESSAGE)`のようにユーザー向けのエラーメッセージを画面に表示する仕組み(上記1)は実装されている。一方で、開発者向けのログ出力(`console.error`など)や、通信失敗時の自動リトライの仕組み(上記2・3)はこのアプリには実装されておらず、今後の改善余地となっている。

---



## 第3章 データベースの基礎（8問）

> 注記: 設問は「tasksテーブル」という前提で書かれているが、このプロジェクトの実際のテーブル名は`cards`／`board_columns`。回答時は自分のプロジェクトのテーブル名に読み替えて書く。



### Q21. データベースとは

**回答:**
・データベースとは：整理された大量のデータを効率よく保存・検索・更新・管理できるように構築された専用のシステムの事です。

・ExcelやCSVで保存する方法と比べて何が優れていますか

1. 大量のデータの高速処理
数百万～数百億件といった大規模なデータでも、インデックスなどの仕組みにより高速に検索や追加・更新が行えます。

2. 同時アクセスの安全性(排他制御)
複数のユーザーやシステムが同時にアクセスしてもデータが破壊されずに、整合性を保ちながら安全に読み書きできる。

3. 厳格なアクセス権限とセキュリティ
ユーザーごとにアクセス権限を設定したり、暗号化やバックアップの仕組みが整っていたりするため安全性が高くなります。

4. データ間の複雑な関連付け(リレーション)
データの重複を防ぎ、テーブル同士を結び付けて効率的に管理できるようになります。

このアプリでは実際にPostgreSQL(`localhost:5432/trello`)を使用しており、上記の4点はまさにこのアプリがPostgreSQLを採用している理由そのものと言える。

### Q22. テーブル・行・列

**回答:**
・Excelシートへの例え
テーブル：Excelファイルの一枚のワークシート(表全体)のことです。
行(レコード)：表の横１行分の事です。(１行分のデータを表す)
列(カラム)：表の縦１列分の事です。(データ項目などを表します)

tasksテーブルでの具体例
・テーブル名：tasks(タスク管理シート)

・行(レコード)
１行目：1｜買い物に行く｜未完了｜2026-08-08
２行目：２｜書類の提出｜完了｜2026-08-08

・列(カラム)
id(タスクID)、title(タスク名)、status(ステータス)、created_at(作成日時)

※このアプリの実際のテーブルは`cards`で、列は`id`・`title`・`description`・`priority`・`due_date`・`column_id`・`sort_order`。`status`や`created_at`という列は実際には存在しない。

### Q23. 主キー（PRIMARY KEY）

**回答:**
・主キーとは何か？
テーブル内の各レコード(行)を一意(ユニーク)に識別するための特別な列(カラム)の事です。

主キーに設定された列には、「重複した値を持てない(一意性)」および「NULL(空文字・未設定)を許容しない(非NULL制約)」という二つの絶対的なルールが適用されること。

・なぜ必要なのか？
データの重複・混同を防ぐため：同じ名前のタスク(例「買い物」が複数回登録された場合など）があっても、idという固有の番号を割り振ることで、コンピュータが「どれを更新・削除すべきか」を確実に特定できるようにするため。

検索・処理速度を上げるため：主キーを設定すると自動的にインデックス(索引)が作成され、該当レコードを高速に検索できるようになるため。

このアプリでは`cards`テーブルの`id`列が主キーであり、`Card.java`の`@Id`アノテーションと`UUID.randomUUID()`による自動生成でこれを実現している。

### Q24. CRUD操作

**回答:**
・CRUDとは何の略ですか？
C：Create（生成・作成）
R：Read（読み取り・取得）
U：Update（更新）
D：Delete（削除）

・それぞれがこのアプリのどの機能に対応するか

Create(生成・作成)
対応機能：タスクの新規登録機能
説明：ユーザーが入力フォームにタイトルや詳細を入力して「登録」ボタンを押した際、tasksテーブルに新しいレコード(タスク行)を１件追加する処理のこと。

Read(読み取り・取得)
対応機能：タスク一覧表示機能/タスク詳細表示機能
説明：画面を開いた際に、tasksテーブルから保存されているタスクの一覧データや特定のタスク１件のデータを取得して画面に表示させる処理のこと。

Update（更新）
対応機能：タスクの編集・状態変更機能
説明：タスクのタイトル変更したり、「未完了」から「完了」へステータスを切り替えた際、特定のidを持つレコードの内容を書き換える処理の事。

Delete（削除）
対応機能：タスクの削除機能
説明：不要になったタスクや完了したタスクの「削除」ボタンを押した際、特定のidを持つレコードをtasksテーブルから完全に抹消する処理の事。

※このアプリの実際のCRUD操作は`CardController`の`createCard`(Create)・`getCards`(Read)・`updateCard`(Update)・`deleteCard`(Delete)に対応する。なお、このアプリに「未完了/完了」のステータス列は存在せず、実際は`priority`(優先度)や`column_id`(どの列にあるか)で状態を管理している。

### Q25. データの制約

**回答:**
・NOT NULL制約
概要：該当する列に「NULL（空データ・値が存在しない状態）」が入ることを禁止する制約。
目的・必要性
データの完全性の保証：「タイトルが空のタスク」など、アプリとして成立しない不完全なデータがDBに登録されるのを防ぐため。
システムエラーの防止：プログラム側でデータを読み込んだ際に、値がnullであることによる予期せぬエラー(JavaのNullPointerExceptionなど)を未然に防ぐため。

・DEFAULT 'medium'制約
概要：レコードを新規登録(INSERT)する際、その列の値が指定されなかった場合に、自動的にデフォルト値(初期値)として`medium`を設定する制約。
目的・必要性：
データ入力の補完・整合性維持：ユーザーがタスクの優先度(priorityなど)を選択しなかった場合でも、自動で標準値(中・medium)が入るため、データが空にならず安定して処理できる。
コードの記述簡略化：アプリケーション側のSQL文で毎回初期値を明示的に指定する必要がなくなるため、開発効率と保守性が向上する。

※このアプリでは`column_id`に`@JoinColumn(nullable = false)`（NOT NULL制約＋外部キー制約）が実際に設定されている。一方、`priority`の`medium`という初期値は、実際にはDBのDEFAULT制約ではなく`Card.java`の`private Priority priority = Priority.MEDIUM;`というJavaコード側の初期値として実装されている。アプリ経由で登録する分には同じ効果が得られるが、DB自体には「値がなければ自動補完する」制約は入っていない点が違いとなる。

### Q26. インデックス

**回答:**
・インデックスとは何か？
データベース内のテーブルから、特定のデータを高速に検索・取得（SELECT）するための索引機能のこと。
対象のカラムにあらかじめインデックスを設定しておくことで、テーブル全体のデータを1行ずつ探す「全件走査(フルスキャン)」を防ぎ、検索処理の負荷とレスポンス時間を大幅に削減できる。

・本の索引に例えて説明してください
インデックスがない場合(フルスキャン)
分厚い専門書の中から「特定の用語」を探すために1ページ目から最後のページまで順番にすべてめくって探すような状態。
ページ数(データ量)が増えるほど探す時間が膨大になること。

インデックスがある場合
巻末にある「五十音順の索引」を見て該当の用語が載っているページ番号を確認し、そのページを直接開くような状態。どれだけ本が分厚くても、目的の箇所をピンポイントかつ一瞬で見つけ出すことができる。

※このアプリでは主キー(`id`)以外に明示的なインデックスは設定されていない。また`CardController`の`keyword`検索は、DBに絞り込みを投げるのではなく全件をJavaに読み込んでから`stream().filter()`で絞り込んでいるため、仮に`title`にインデックスを付けてもこの検索自体は速くならない点も実装上の注意点。

### Q27. トランザクション

**回答:**
・トランザクションとは何ですか。
データベースにおいて、「複数の関連する処理をひとまとめにした分割不可能な作業単位」のこと。
「すべて成功すれば確定(コミット)」し、「途中で１つでも失敗すればすべて処理前の状態に戻す(ロールバック)」という不可分性(All or Nothing)を保証し、データの不整合(矛盾)を防ぐ仕組み。

・タスクを別カラムへドラッグ移動する操作で、なぜ重要ですか。
看板ボードなどでタスクを別カラム(例：「未着手」から「進行中」)へドラッグ移動する際、内部では複数のデータ更新が同時に発生するため。

具体的に発生する処理の事例
1. 移動したタスクのステータス更新(例：statusをin_progressに変更)
2. 移動元カラムに残ったタスクの並び順(表示順序/position)の繰り上げ更新
3. 移動先カラムに存在する他のタスクの並び順の繰り下げ更新

トランザクションが必要な理由
もしトランザクションを使用せずに処理の途中でエラーが起きた場合、「タスクの移動だけ完了して並び順が更新されない」「同じ表示順位(position)が重複してタスクが重なって消える」といった画面表示やデータの重大な不整合が発生してしまう。

これら複数の更新処理を１つのトランザクションで囲むことで、エラーが起きた際も移動前の完全な状態へ安全に巻き戻し(ロールバック)、データの整合性を保つことができるため極めて重要である。

※このアプリの実際の`CardController`(`updateCard`)は、移動したカード1件の`order`を更新するのみで、移動元・移動先カラムの他のカードの並び順を一括で繰り上げ／繰り下げする処理や`@Transactional`は実装されていない。この回答は「本来そうした複数更新の処理を実装するならトランザクションが必須になる」という、今後の設計に対する正しい洞察と言える。

### Q28. SQLとORM

**回答:**
・SQLとはどのようなものですか？
リレーショナルデータベース(RDBMS)に対して、データの検索・追加・更新・削除などの操作や、テーブルの定義・管理を行うための標準的なデータベース問い合わせ言語。
SELECT、INSERT、UPDATE、DELETEなどの構文を用いて、データベースと直接対話を行う。

・ORMの役割とは何ですか？
ORM（Object-Relational Mapping：オブジェクト関係マッピング）は、オブジェクト指向言語(JavaやRubyなど)の「オブジェクト」と、データベースの「テーブル(リレーショナルデータ)」を自動的に相互変換・橋渡しする仕組み。

主な役割・メリット
・コードの簡略化・可読性向上：生のSQL文を直接書くことなく、プログラミング言語のメソッド呼び出しでデータベース操作ができる。

・SQLインジェクション対策(セキュリティ向上)：パラメータの自動エスケープ処理が行われるため、脆弱性を防ぎやすい。

・DB依存の低減：異なるデータベース(MySQL、PostgreSQLなど)へ移行する際も、アプリ側のコード修正を最小限に抑えられる。

このアプリでは`CardRepository`が`JpaRepository`を継承しているだけで、`save()`・`findById()`・`deleteById()`といった基本操作がSQL文を書かずに使えるようになっており、まさにORM(Spring Data JPA)による恩恵と言える。

---



## 第4章 開発プロセスとチーム開発（4問）



### Q29. バージョン管理（Git）

**回答:**
・Gitなどのバージョン管理システムとは何ですか？
ファイルの変更履歴(誰が、いつ、どの行を、どのように変更したか)を記録・管理するためのシステム。
過去の状態を保存(コミット)しておくことで、いつでも過去の任意の時点にファイルを復元したり、変更差分を正確に比較・確認したりできる。

・なぜ必要ですか？
安全なコード管理：バグや不具合が発生した際に、正常に動いていた過去の状態へ即座にロールバック(復元)出来るため。
スムーズなチーム開発：複数人で同時に別々の機能開発を行っても、コードの競合を検知・統合(マージ)しやすくなるため。
作業履歴の可視化：コミットメッセージにより「なぜその変更を行ったのか」という開発の意図や経緯をチーム全体で共有・追跡できるため。

・使わないとどんな問題がおきますか？
ファイルの先祖返り・上書き事故：複数人で同じファイルを編集した際、あとから保存した人のデータで他人の書いたコードが意図せず消去・上書きされてしまう。
原因究明や復元が困難：バグが発生した際に「どの変更が原因か」がわからず、手作業で変更前のコードを思い出しながら修正しなければならなくなる。
ファイル管理の煩雑化：main_最新.javaのように命名したバックアップファイルが乱立し、どれが本当に最新で正しいコードなのか判断つかなくなる。

このプロジェクトでも`git log`を見ると、機能追加のたびに「カード編集フォームにステータス選択を追加し、変更時に優先度順で自動並び替え (#12)」のように1つのコミットとして記録が積み重なっており、CLAUDE.mdのルールに沿ってIssue番号付きのブランチで並行作業を安全に進めている。

### Q30. ブランチとPull Request

**回答:**
・ブランチとは何ですか？
本番稼働用や開発ベースのコード(mainなど)から作業履歴の流れを枝分かれ(分岐)させて、他の開発者に影響を与えずに並行して開発を行うための機能の事。
機能追加やバグ修正ごとに独立したブランチを作成して作業することで、安全にコードの変更やテストが行える。

・Pull Requestの目的は何ですか？
自分のブランチで行った変更を、共有・本番ブランチ(mainやdevelopなど)に取り込んでもらう(マージする)よう他の開発者に依頼・通知をすること。
主な目的：
コードレビューの実施：マージ前に他のメンバーが変更差分を確認し、バグの混入防止、品質維持、セキュリティチェックを行う。
変更経緯の記録・共有：なぜその修正を行ったのか、どのようなテストを実施したのかをチーム全体に可視化して記録を残す。

・なぜ全員がmainに直接変更しないんですか？
動かなくなるリスク(品質低下)を防ぐため：mainブランチは常に安定稼働・リリース可能な状態を保つ必要がある。全員が直接変更プッシュをすると、未完成のコードやバグが即座に本番環境やチーム全体に波及してしまう。

コンフリクト(競合)や上書き事故を防ぐため：複数人が同時に同じファイルを編集してmainに直接反映すると、コードが衝突して互いの作業を消してしまう危険性が極めて高いため。

レビューを通す安全な開発フローを作るため：個別のブランチで開発し、Pull Requestによるレビューと自動テストを通過した物だけをmainに統合することで、高いコード品質と安全性を維持するため。

このプロジェクトの`CLAUDE.md`にも「mainブランチへの直接pushは禁止」「Pull Requestを経由してmainにマージする」と明記されており、GitHub側でもブランチ保護ルール(`enforce_admins: true`)が実際に設定されている。ブランチ名も`feature/11-status-select-auto-sort`のようにIssue番号付きの命名規則に沿って運用されている。

### Q31. コミットメッセージの重要性

**回答:**
・良いコミットメッセージを書くことがなぜ重要なんですか？

変更の「目的(なぜ・何をしたのか)」を即座に把握するため：
コードの差分(diff)だけを見ても意図が分かりにくいため、メッセージで「何のために変更したのか」を明確にすることで、他のメンバーや将来の自分がスムーズに理解できる。

コードレビューの効率と精度の向上のため：
Pull Request を確認するレビュアーがコミット単位で変更の文脈を追えるようになり、レビュー時間の短縮やバグの見落とし防止につながる。

障害発生時の原因特定と復元(ロールバック)を容易にするため：
バグが発生した際、コミット履歴(git log や git blame)から「どのコミットが原因か」を素早く特定し、問題のあるコミットだけを安全に取り消す(git revert)ことができる。

プレフィックス(feat:、fix:、refactor:など)による分類の明確化：
例のように接頭辞を用いることで、新機能の追加なのか、不具合修正なのか、リファクタリングなのかがパッと見で判別でき、リリースノートの自動生成などにも役立つ。

このプロジェクトの`git log`を見ても「カード編集フォームにステータス選択を追加し、変更時に優先度順で自動並び替え (#12)」のように対応するIssue番号が含まれており、`chore:`のようなプレフィックスも実際に使われている。これはCLAUDE.mdの「PRの説明に対応するIssueをClosesの形式で記載する」ルールとも連動している。

### Q32. なぜ技術的な仕組みを理解すべきか

**回答:**
AIがコードを出力できる時代であっても、ネットワーク、データベース、Webアプリケーションの仕組みなどの基礎技術を理解すべき理由は以下の4点です。

1. AIが生成したコードの「妥当性」と「安全性」を判断するため(レビュー力)
AIは一見動くコードを素早く出力しますが、セキュリティ上の脆弱性(SQLインジェクションや認証不備)や、データベースのN＋1問題といったパフォーマンス低下を招くコードを生成することもあります。仕組みを理解していなければ、出力されたコードが「本番環境で安全に動作するか」を人間が検証・判断できません。

2. 複雑な障害発生時の原因特定と解決のため(トラブルシューティング力)
システム障害が起きた際に、「ネットワークの疎通問題なのか」「DBのコネクションやデッドロックなのか」「アプリのロジックエラーなのか」を切り分けるには、システム全体の通信の流れや構造の理解が不可欠です。原因の切り分けができなければ、AIに対して適切な状況説明や修正の指示を出すこともできません。

3. AIへの適切な前提条件とプロンプトを与えるため(指示出しの精度)
最適な実装をAIに作らせるには、「RESTfulな設計」「インデックスを考慮したクエリ」「ステートレスなセッション管理」など、技術的な要件を人間が正しく定義して伝える必要があります。基礎知識がなければ、AIの能力を最大限に引き出す指示を出せません。

4. 全体最適なシステム設計とトレードオフの判断を行うため
アプリケーションの要件に応じて、「どのプロトコルを使うべきか」「RDBMSとNoSQLのどちらを選ぶべきか」「キャッシュをどこに配置するか」といったアーキテクチャの選定・設計判断は、ビジネス要件と技術的制約を理解した人間にしか担えない重要な役割であるためです。