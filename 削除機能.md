# 削除機能：カードの削除

作成日: 2026-08-06
関連ドキュメント: [要件定義書](./要件定義書.md)（3-2. カードの削除）／[設計書](./設計書.md)

フェーズ2（バックエンド・DB化）時点での、カード削除機能の実装内容をまとめる。削除確認ダイアログ（[Issue #13](https://github.com/kuroda1995/github-demo/issues/13) / [PR #14](https://github.com/kuroda1995/github-demo/pull/14)）を含む。

## 1. できること

- カードの「×」ボタンを押すと、確認ダイアログが表示される
- ダイアログで「OK」を選んだ場合のみ、カードがDBから完全に削除される（**物理削除**）
- 「キャンセル」を選んだ場合はカードは削除されず、そのまま残る

## 2. 物理削除である理由（論理削除にしていない理由）

削除されたカードにも「削除済みフラグ」を立てて残しておく方式を**論理削除**と呼ぶが、このアプリでは採用していない。論理削除にすると、削除したはずのカードの行がDBに残り続け、使わなくなったデータが際限なく蓄積していく（＝DBの容量を消費し続ける）。

このアプリは個人のタスク管理用で、削除したカードを後から復元したい・削除履歴を残したいという要件も無いため、削除ボタンを押したカードはDBから**実際に行ごと削除**するシンプルな実装（物理削除）にしている。削除前に確認ダイアログを挟むことで、誤操作による意図しない削除を防いでいる。

## 3. 画面操作の流れ

1. 削除したいカードの「×」ボタンを押す
2. 「「（カードのタイトル）」を削除しますか？この操作は取り消せません。」という確認ダイアログが表示される
3. 「OK」を押すとカードが列から消える（DBからも削除される）。「キャンセル」を押すと何も起きない

## 4. 関連ファイル（フロントエンド）

| ファイル | 役割 |
|----------|------|
| [Card.jsx](./frontend/src/components/Card.jsx) | 「×」ボタンのクリックハンドラ。`window.confirm()`で確認ダイアログを表示し、OKが押された場合のみ`onDelete(card.id)`を呼ぶ |
| [useCards.js](./frontend/src/hooks/useCards.js) | `deleteCard(id)`：削除APIを呼び出し、成功したら画面の状態（`cards`）から該当カードを取り除く |
| [cardsApi.js](./frontend/src/api/cardsApi.js) | `deleteCard(id)`：`DELETE /api/cards/{id}` を呼び出す薄いラッパー |

確認ダイアログはブラウザ標準の`window.confirm()`を使っている（専用のモーダルUIコンポーネントは作っていない）。

## 5. 関連ファイル（バックエンド）

| ファイル | 役割 |
|----------|------|
| [CardController.java](./backend/src/main/java/com/example/trello/controller/CardController.java) | `DELETE /api/cards/{id}`（`deleteCard`）：対象IDが存在するか確認したうえで`cardRepository.deleteById(id)`を実行する |
| [CardRepository.java](./backend/src/main/java/com/example/trello/repository/CardRepository.java) | Spring Data JPAの`JpaRepository`を継承。`deleteById`はJPAが標準で提供するメソッドで、対象の行に対してSQLの`DELETE`文を実行する |

## 6. API仕様

| メソッド | パス | レスポンス |
|----------|------|------------|
| DELETE | /api/cards/{id} | 204 No Content（成功）／404 Not Found（存在しないIDの場合） |

- レスポンスボディは無し
- 削除後、同じIDで再度GET・PUT・DELETEを呼んでも404になる（DBに行が残っていないため）
