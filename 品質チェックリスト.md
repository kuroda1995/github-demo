# 品質チェックリスト：Trello風タスク管理アプリ（仮）

作成日: 2026-08-07
関連ドキュメント: [要件定義書](./要件定義書.md)／[設計書](./設計書.md)／[技術スタック](./技術スタック.md)／[品質チェック記録](./品質チェック記録.md)／[.claude/skills/quality-review](./.claude/skills/quality-review/SKILL.md)

## 1. このドキュメントについて

「動く」ことと「品質が良い」ことは別の話。このアプリはスクール課題（学習目的）だが、実務のコードレビューで実際に見られる観点を体験するために、以下の4つの軸でチェックリストを作った。

1. **フロントエンド（React/Vite）の作り方が、Reactの定石から大きく外れていないか**
2. **バックエンド（Spring Boot）の作り方が、Spring Bootの定石から大きく外れていないか**
3. **Lint・テスト・CIなど、"人が見なくても機械が気づいてくれる仕組み"がどこまで整っているか**
4. **ドキュメント（要件定義書・設計書・各機能ドキュメント）と実際のコードの内容が一致しているか**

チェックした結果は都度直すのではなく、まず [品質チェック記録.md](./品質チェック記録.md) に記録してから、対応するかどうか・いつ対応するかを決める運用にする（CLAUDE.mdのルール上、コードを直す場合はIssueを立ててから着手するため）。

このチェックリストは`.claude/skills/quality-review/SKILL.md`（Claude Codeが自動でチェックする際の手順書）のベースにもなっている。人が読んで学ぶための詳しい版がこのファイル、Claude Codeが機械的に実行するための短い版がSKILL.md、という役割分担。

判定は3段階で記録する。

| 記号 | 意味 |
|------|------|
| ✅ | 問題なし。定石通り、または今回の学習目的の範囲では妥当な選択 |
| ⚠️ | 今すぐ直す必要はないが、実務なら改善したい・理由を説明できるようにしておきたい点 |
| ❌ | バグにつながる、または明らかに定石から外れていて直したほうがよい点 |

## 2. フロントエンド（React/Vite）チェックリスト

| ID | チェック項目 | なぜ確認するか | 確認方法 |
|----|--------------|------------------|----------|
| FE-1 | Lintツールが導入されているか | タイポや未使用変数、Reactのお作法違反（hooksの呼び出し順序など）を人間が見る前に機械的に検出するため | `package.json`の`devDependencies`と`scripts.lint`、設定ファイル（`.eslintrc*`や`.oxlintrc.json`など）の有無を確認する |
| FE-2 | Lintのルールが主要な観点をカバーしているか | ツールを入れただけで満足せず、実際に効いているルールを見る（特に`react-hooks/exhaustive-deps`のような、hooksの依存配列の抜け漏れを検出するルール） | Lint設定ファイルの`rules`セクション、または使っているプラグインの推奨ルールセット（`plugin:recommended`等）を確認する |
| FE-3 | テストコードがあるか | 手で毎回動作確認する代わりに、壊れたことを機械的に検出できるようにするため | `*.test.jsx`/`*.spec.jsx`やテスト用ディレクトリの有無、`devDependencies`にVitest/Jest/React Testing Library等があるかを確認する |
| FE-4 | コンポーネントの責務が1ファイル1役割に分かれているか | 1つのコンポーネントが「表示」と「状態管理」と「API通信」を全部やっていると、変更の影響範囲が読みにくくなるため | 各コンポーネントファイルを開き、他のコンポーネントの表示ロジックやAPI呼び出しを直接抱え込んでいないかを見る |
| FE-5 | 状態管理が一元化されているか（Props Drillingが浅く済んでいるか、無秩序なグローバル状態が無いか） | 同じデータを複数箇所で別々に持ってしまうと、表示がズレるバグの温床になるため | 状態を持っている場所（`useState`/カスタムフック/Context）を洗い出し、「どこが正の情報源か」が1箇所に決まっているかを見る |
| FE-6 | XSS対策ができているか（`dangerouslySetInnerHTML`を安易に使っていないか） | ユーザー入力をそのままHTMLとして描画すると、悪意あるスクリプトが実行される脆弱性になるため | `dangerouslySetInnerHTML`をコード全体でgrepし、使っている場合はその入力元がサニタイズ済みかを確認する |
| FE-7 | 使われていないコード（デッドコード）が残っていないか | 読む人が「これはどこかで使われているはず」と誤解し、無駄な調査時間を生むため | 関数・変数をエクスポートしている箇所について、実際にimportされているかを検索する |
| FE-8 | APIエラー時の挙動が一貫しているか | 通信エラー時に画面が真っ白になったり、何も起きないように見えると使う人が混乱するため | fetch/axios等の呼び出し箇所をすべて洗い出し、失敗時にユーザーへ表示されるか・どこかに共通化されているかを見る |
| FE-9 | 環境変数・機密情報の管理が適切か | APIキーやDBのパスワードなどをうっかりGitにコミットして公開してしまう事故を防ぐため | `.env`系ファイルが`.gitignore`に入っているか、実際に機密情報が含まれていないかを確認する |
| FE-10 | ファイル構成・命名規則に一貫性があるか | 同じ種類のファイルが人によってバラバラの置き場所・名前になると、後から探しにくくなるため | `components/`・`hooks/`・`api/`・`utils/`などのディレクトリ構成と、そこに置かれているファイルの粒度・命名が揃っているかを見る |

## 3. バックエンド（Spring Boot）チェックリスト

| ID | チェック項目 | なぜ確認するか | 確認方法 |
|----|--------------|------------------|----------|
| BE-1 | レイヤードアーキテクチャ（Controller → Service → Repository）になっているか | Controllerが「HTTPの受け口」、Serviceが「業務ロジック」、Repositoryが「DBアクセス」と役割を分けることで、後からロジックだけをテストしたり差し替えたりしやすくなるため | `controller`パッケージが`repository`を直接呼んでいないか、業務ロジック（バリデーションや計算）がController内に散らばっていないかを見る |
| BE-2 | 共通例外ハンドラ（`@ControllerAdvice`/`@RestControllerAdvice`）があり、エラーレスポンスの形が統一されているか | エラーの返し方がAPIごとバラバラだと、フロントエンドが「このAPIは何が返ってくるか」を個別に覚えないといけなくなるため | `@ExceptionHandler`や`@ControllerAdvice`を検索する。無い場合は、意図的に投げている例外（`ResponseStatusException`等）と、想定外の例外（`IllegalArgumentException`等）とで返ってくるステータスコードが揃っているかを実際にAPIを叩いて確認する |
| BE-3 | 入力バリデーションが主要な項目を網羅しているか | 不正な値がDBまで届いてしまうと、後工程で原因調査が大変になるため | DTOクラス（例：`CardRequest`）の各フィールドに`@NotBlank`等のBean Validationアノテーションが付いているか、`@Valid`がController側で使われているかを見る |
| BE-4 | レスポンスがEntity（DBのテーブルに対応するクラス）そのものではなく、専用のDTOになっているか | Entityをそのまま返すと、DBのテーブル構造を変えただけでAPIの見た目まで変わってしまい、フロントエンドが壊れるリスクがあるため | Controllerの戻り値の型がEntityクラスそのものになっていないかを見る（小規模なら意図的な簡略化として許容できる場合もある） |
| BE-5 | Lint・静的解析ツールが導入されているか（Checkstyle／Spotless／PMD等） | フォーマットの揺れやアンチパターンを、レビューする人が毎回目視で指摘しなくて済むようにするため | `build.gradle`（または`pom.xml`）に該当プラグインが定義されているかを確認する |
| BE-6 | CI（GitHub Actions等）でビルド・Lint・テストが自動実行されているか | 手元では動いていても、他の環境や別ブランチでは壊れている、という事故を防ぐため | リポジトリの`.github/workflows/`ディレクトリの有無を確認する |
| BE-7 | テストが主要なAPI・分岐（正常系／異常系）をカバーしているか | コンテキストが起動するかどうかのテストだけでは、実際のAPIの振る舞い（400/404/正常系）は保証されないため | `src/test/`配下のテストクラスを確認し、`@WebMvcTest`等でController単位のテストが書かれているか、異常系（不正な入力・存在しないID）のテストがあるかを見る |
| BE-8 | 認証情報・接続情報の扱いが適切か | DBのパスワード等を平文でコミットする習慣がつくと、実務で本番環境の情報を漏らす事故につながるため | `application.properties`等にハードコードされた値が、ローカル学習用途の範囲に留まっているか（本番用の値が紛れ込んでいないか）を確認する |
| BE-9 | 検索・絞り込みがDBのクエリで行われているか（アプリ側での全件取得＋ループ処理になっていないか） | データ件数が増えたときに、全件をメモリに読み込んでからフィルタする実装は極端に遅くなるため | `findAll`してから`stream().filter()`しているだけの箇所がないか、`@Query`や条件付きの検索メソッドを使えないかを見る（学習用の少量データでは実害が出にくいので、優先度は低くてよい） |
| BE-10 | 複数の更新をまたぐ処理に、トランザクション管理（`@Transactional`）の考慮があるか | 複数のDB更新のうち一部だけ成功してしまうと、データの整合性が崩れるため | 1つの業務操作の中で複数回`save`/`delete`している箇所を洗い出し、途中で失敗した場合にデータがどうなるかを考える |

## 4. ドキュメント整合性チェックリスト

| ID | チェック項目 | なぜ確認するか | 確認方法 |
|----|--------------|------------------|----------|
| DOC-1 | 要件定義書の機能一覧・画面仕様・ユースケースが実装と一致しているか | ドキュメントが古いまま放置されると、後から読んだ人（未来の自分も含む）が実際と違う仕様を信じてしまうため | 要件定義書の各項目を、対応するコンポーネント・画面操作と実際に突き合わせる |
| DOC-2 | 設計書のAPI設計表が、実際のエンドポイント・パラメータ・レスポンスと一致しているか | フロントエンドとバックエンドの契約（インターフェース）がズレると、連携時にバグの原因になるため | Controllerの`@GetMapping`等のパスとパラメータを、設計書のAPI設計表と突き合わせる |
| DOC-3 | 個別機能ドキュメント（新規登録機能.md等）が、対応するコンポーネント・APIの現状と一致しているか | 機能追加のたびにドキュメントを更新し忘れると、ドキュメントの数が増えるほど信頼性が下がっていくため | 各ドキュメントに書かれているファイル名・関数名が、実際のコードに存在するか（リネームされていないか）を確認する |
| DOC-4 | READMEの「主な機能」「開発フェーズの状態」が実際の状態と一致しているか | READMEは最初に読まれるドキュメントなので、ここが古いと第一印象からズレてしまうため | READMEに書かれている機能一覧・フェーズの状態を、実際に動かして（または最新のコードを見て）確認する |
| DOC-5 | ドキュメント間の相互リンクが切れていないか | リンク切れがあると、ドキュメントを辿って理解しようとした人がそこで詰まってしまうため | 各`.md`ファイル内の相対リンク（`./〇〇.md`）が実際に存在するファイルを指しているかを確認する |

## 5. チェックの進め方

1. 対象ファイルを特定する（`frontend/src/`、`backend/src/main/java/...`、ドキュメント一式）
2. フロントエンド（FE-1〜FE-10）とバックエンド（BE-1〜BE-10）を並行してチェックする
3. ドキュメント整合性（DOC-1〜DOC-5）をチェックする
4. 見つかった内容を[品質チェック記録.md](./品質チェック記録.md)に日付・判定（✅/⚠️/❌）付きで記録する
5. ドキュメント側の問題は「実装を正として」ドキュメントを修正する（CLAUDE.mdのIssue→ブランチ→PRの流れに従う）
6. コード側の❌（バグ相当）は、影響範囲が小さいものから優先してIssue化し、実装の振る舞いを変えない範囲で修正する
7. コード側の⚠️（設計・構造の改善余地）は、すぐには直さず記録だけ残し、対応するかどうかは別途相談する（学習目的のプロジェクトなので、あえて直さず「なぜこうなっているか説明できる」状態にしておく選択肢もある）

## 6. 用語解説（学習用）

- **レイヤードアーキテクチャ**：処理を「入り口（Controller）」「業務ロジック（Service）」「データアクセス（Repository）」のように役割ごとの層に分ける設計の考え方。層を分けておくと、例えば「業務ロジックだけをテストしたい」「DBをPostgreSQLから別のものに変えたい」といった変更が、他の層に影響を与えにくくなる。
- **`@ControllerAdvice`／`@RestControllerAdvice`**：Spring Bootで、複数のControllerに共通する例外処理を1箇所にまとめて書くための仕組み。これが無いと、同じような「存在しない場合は404を返す」処理を、Controllerのメソッドごとに書き直すことになる。
- **DTO（Data Transfer Object）**：データをやり取りするためだけに使う入れ物のクラス。DBのテーブルに対応するクラス（Entity）とAPIのレスポンスを分けておくと、DBの都合とAPIの都合を別々に変更できる。
- **CI（継続的インテグレーション）**：コードをリポジトリにpushするたびに、ビルド・Lint・テストを自動実行する仕組み。GitHub Actionsなどで構築する。「手元では動いていたのに、他の人の環境では動かない」という事故を早期に検知できる。
- **XSS（クロスサイトスクリプティング）**：ユーザーが入力した文字列が、そのままHTML／JavaScriptとしてページに埋め込まれてしまい、悪意あるスクリプトが実行されてしまう脆弱性。Reactは通常のJSX展開（`{value}`）であれば自動でエスケープしてくれるため安全だが、`dangerouslySetInnerHTML`を使うとその保護が外れる。
- **デッドコード**：定義されているが、どこからも呼ばれていないコード。動作に影響は無いが、読む人を惑わせるため見つけたら削除するのが望ましい。
