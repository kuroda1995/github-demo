# 検索機能：カードの検索・並び替え

作成日: 2026-08-07
関連ドキュメント: [要件定義書](./要件定義書.md)（3-1. 必須機能 No.8／6-4. 検索・並び替えの仕様）／[設計書](./設計書.md)

フェーズ2（バックエンド・DB化）時点での、カード検索・並び替え機能の実装内容をまとめる（[Issue #6](https://github.com/kuroda1995/github-demo/issues/6)）。

## 1. できること

- 画面上部の検索バーにキーワードを入力すると、タイトル・説明文にそのキーワードを含むカードだけが各列に表示される（大文字小文字は区別しない）
- 優先度（高／中／低）でカードを絞り込むこともできる。キーワードと優先度は組み合わせて指定できる
- 絞り込み中は、該当するカードの件数が検索バー付近に表示される
- 「クリア」ボタンで絞り込みを解除し、全カードの表示に戻せる
- 各列にある「優先度順」「期限順」ボタンを押すと、その列の中のカードだけが指定した基準で並び替わる（列単位の操作）

## 2. 画面操作の流れ

### 検索

1. 検索バーにキーワードを入力する、または優先度を選択する
2. 入力が止まってから一定時間後（デバウンス）に自動的に検索が実行され、条件に一致するカードだけが表示される
3. 「クリア」ボタンを押すと、キーワード・優先度の指定がリセットされ、全カードの表示に戻る

### 並び替え

1. 並び替えたい列の「優先度順」または「期限順」ボタンを押す
2. その列の中のカードが指定した基準で並び替わり、`order`（並び順）がDBにも反映される
3. ボタンを押した瞬間の一回限りの操作で、以降にカードを追加・編集しても自動では再ソートされない（カードのステータス変更時の自動並び替えとは別の仕組み。詳細は[更新機能](./更新機能.md)を参照）

## 3. 関連ファイル（フロントエンド）

| ファイル | 役割 |
|----------|------|
| [SearchBar.jsx](./frontend/src/components/SearchBar.jsx) | キーワード入力欄・優先度セレクト・クリアボタン。入力変更後300ms（`DEBOUNCE_MS`）でデバウンスして`onSearch`を呼ぶ |
| [SortControls.jsx](./frontend/src/components/SortControls.jsx) | 「優先度順」「期限順」ボタン。押すと`onSort(key)`を呼ぶ |
| [Board.jsx](./frontend/src/components/Board.jsx) | `SearchBar`を配置し、`useCards`の`searchCards`を渡す。絞り込み中は該当件数（`検索結果: N件`）を表示する |
| [Column.jsx](./frontend/src/components/Column.jsx) | 列ごとに`SortControls`を配置し、`useCards`の`sortColumn`を列IDと紐づけて呼ぶ |
| [useCards.js](./frontend/src/hooks/useCards.js) | `searchCards(filters)`：`GET /api/cards`をクエリパラメータ付きで呼び直し、結果で`cards`を置き換える。`sortColumn(columnId, sortKey)`：列内のカードを並び替えて、影響を受けたカードだけをバックエンドにPUTする |
| [cardsApi.js](./frontend/src/api/cardsApi.js) | `getCards(filters)`：`keyword`・`priority`・`columnId`をクエリパラメータとして`GET /api/cards`に付与するラッパー |
| [cardUtils.js](./frontend/src/utils/cardUtils.js) | `sortColumnCards(cards, columnId, sortKey)`：指定した列のカードを`sortKey`（`"priority"` \| `"dueDate"`）で並び替え、`order`を0,1,2...に振り直す実処理 |

## 4. 関連ファイル（バックエンド）

| ファイル | 役割 |
|----------|------|
| [CardController.java](./backend/src/main/java/com/example/trello/controller/CardController.java) | `GET /api/cards`：全カードを`order`昇順で取得したあと、`keyword`・`priority`・`columnId`の各クエリパラメータが指定されていれば、それぞれJavaのstreamでインメモリ絞り込みを行う（DBクエリでの絞り込みではない） |

並び替え専用のAPIは無い。並び替えはフロントエンド側で計算し、影響を受けた各カードを個別に`PUT /api/cards/{id}`することでDBに反映する。

## 5. API仕様

| メソッド | パス | クエリパラメータ | レスポンス |
|----------|------|-------------------|------------|
| GET | /api/cards | `keyword`（任意、タイトル・説明文の部分一致）／`priority`（任意、`high`/`medium`/`low`）／`columnId`（任意） | 200 OK + 条件に一致するカードの配列（`order`昇順） |

- 複数のクエリパラメータを指定した場合はAND条件で絞り込まれる
- パラメータを何も指定しない場合は全カードを返す（通常の一覧取得と同じ）
