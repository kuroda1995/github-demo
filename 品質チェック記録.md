# 品質チェック記録：Trello風タスク管理アプリ（仮）

関連ドキュメント: [品質チェックリスト](./品質チェックリスト.md)（各チェック項目ID・判定基準の説明はこちらを参照）

このファイルは、[品質チェックリスト.md](./品質チェックリスト.md)に沿ってチェックを実施するたびに、実施日を見出しにして記録を追記していくもの。過去の記録は書き換えず、状況が変わったら新しい日付の見出しで追記する（「〇〇は△△回目のチェックで直った」という経緯が後から追えるようにするため）。

---

## 2026-08-07 初回品質チェック

- **きっかけ**: フロントエンド（React/Vite）・バックエンド（Spring Boot）の実装全体に対する初めての品質チェックを実施。あわせて、要件定義書・設計書・各機能ドキュメントと実装の内容が一致しているかも確認した。
- **チェック範囲**: `frontend/src/`一式、`backend/src/main/java/com/example/trello/`一式、`要件定義書.md`・`設計書.md`・`技術スタック.md`・`新規登録機能.md`・`更新機能.md`・`削除機能.md`・`README.md`
- **関連Issue/PR**: [Issue #17](https://github.com/kuroda1995/github-demo/issues/17)（ドキュメント整合性修正＋品質レビュー用スキル追加）／ [PR #18](https://github.com/kuroda1995/github-demo/pull/18) ／ [Issue #19](https://github.com/kuroda1995/github-demo/issues/19)（エラーレスポンス統一）／ [PR #20](https://github.com/kuroda1995/github-demo/pull/20)

### 結果サマリ

| ID | 判定 | 内容 | 対応状況 |
|----|------|------|----------|
| FE-1 | ✅ | `oxlint`が導入され、`npm run lint`で実行できる | 対応不要 |
| FE-2 | ⚠️ | `.oxlintrc.json`のルール指定が`react/rules-of-hooks`等2件のみと最小限 | 未対応（今後の課題） |
| FE-3 | ❌ | Vitest等のテストフレームワークが未導入で、テストファイルが0件 | 未対応（今後の課題） |
| FE-4 | ✅ | `Board`/`Column`/`Card`/`CardForm`等、コンポーネントの責務が分かれている | 対応不要 |
| FE-5 | ✅ | `useCards`フックにカード・列の状態を一元化し、Props Drillingも浅く一貫している | 対応不要 |
| FE-6 | ✅ | `dangerouslySetInnerHTML`は使われておらず、すべて通常のJSX展開で描画している | 対応不要 |
| FE-7 | ⚠️ | `cardUtils.js`の`makeId()`が未使用のデッドコードとして残存（バックエンド接続前の名残） | 未対応（今後の課題） |
| FE-8 | ⚠️ | 共通fetchラッパー（`http.js`）はあるが、失敗時の詳細（ステータスコード等）はユーザー表示用の固定メッセージに置き換わり捨てられている | 未対応（今後の課題） |
| FE-9 | ⚠️ | `frontend/.env`が`.gitignore`の対象外（現状は機密情報なし） | 未対応（今後の課題） |
| FE-10 | ⚠️ | `getColumns()`が`cardsApi.js`に同居しており、リソース単位でファイルが分かれていない | 未対応（今後の課題） |
| BE-1 | ⚠️ | Service層が無く、Controllerが直接Repositoryを呼んでいる（学習用途としては意図的な簡略化と解釈できる） | 未対応（今後の課題） |
| BE-2 | ❌→✅ | 不正な`priority`値が捕捉されず500になる／`deleteCard`の404の返し方が`updateCard`と異なる、という不整合を確認 | **対応済み**（PR #20：`GlobalExceptionHandler`追加、`deleteCard`を`ResponseStatusException`に統一） |
| BE-3 | ⚠️ | `CardRequest`は`title`・`columnId`のみ`@NotBlank`。`dueDate`や`order`に制約が無い | 未対応（今後の課題） |
| BE-4 | ⚠️ | レスポンス専用DTOが無く、`Card`/`BoardColumn`エンティティをそのまま返している | 未対応（今後の課題） |
| BE-5 | ❌ | Checkstyle/Spotless/PMD等の静的解析ツールが未導入（フロントの`oxlint`に相当するものが無い） | 未対応（今後の課題） |
| BE-6 | ❌ | `.github/workflows/`が存在せず、Lint・テストがCIで自動実行されていない | 未対応（今後の課題） |
| BE-7 | ❌→⚠️ | テストが`TrelloBackendApplicationTests`（コンテキストロードのみ）の1件だけだった | **一部対応**（PR #20：`CardControllerTest`を追加し、BE-2の異常系2件をカバー。正常系や他Controllerのテストはまだ無い） |
| BE-8 | ⚠️ | `application.properties`にDB接続情報（`postgres`/`postgres`）がハードコード。ローカル学習用途の範囲では許容 | 未対応（学習用途の範囲として許容） |
| BE-9 | ⚠️ | `GET /api/cards`の検索・絞り込みが全件取得後のJavaストリーム処理。現状のデータ量（学習用途）では実害なし | 未対応（今後の課題、優先度低） |
| BE-10 | ⚠️ | `@Transactional`が明示的に使われていない。現状はSpring Data JPAのリポジトリ単位のトランザクションで問題なく動作している | 未対応（今後の課題、優先度低） |
| DOC-1 | ❌→✅ | 要件定義書の「削除方法」が実装（確認ダイアログあり）と矛盾／「編集方法」にステータス変更が未記載／検索・並び替え機能が丸ごと未記載、という3点を確認 | **対応済み**（PR #18で修正） |
| DOC-2 | ❌→✅ | 設計書のAPI設計表に`GET /api/cards`のクエリパラメータ（keyword/priority/columnId）が未記載 | **対応済み**（PR #18で修正） |
| DOC-3 | ⚠️→✅ | 検索・並び替え機能について、他の機能（削除・新規登録・更新）と同形式の個別ドキュメントが存在しなかった | **対応済み**（PR #18で`検索機能.md`を新規作成） |
| DOC-4 | ✅ | READMEの「主な機能」に検索・並び替えの記載は既にあった（要件定義書側が追いついていなかった） | 対応不要（PR #18でリンク一覧に検索機能.mdを追加） |
| DOC-5 | ✅ | 各ドキュメントの相互リンクは切れていなかった | 対応不要 |

### 良かった点

- フロントエンドは状態管理の一元化・XSS対策・コンポーネント分割など、Reactの定石に沿っている（FE-4〜FE-6）
- ドキュメントの矛盾・記載漏れは、実装を正としてすべてPR #18で修正済み（DOC-1〜DOC-3）
- バックエンドの明らかなバグ（不正な`priority`が500になる／404の返し方の不統一）はPR #20で修正済み、回帰テストも追加した（BE-2、BE-7の一部）

### 今後の課題（未対応のまま）

優先度が高いと考えられる順：

1. **BE-5／BE-6**: バックエンドにLint・静的解析ツールとCIが無く、品質チェックが人力（このドキュメント）に依存している
2. **FE-3／BE-7**: フロントエンドのテストが0件、バックエンドのテストも薄い（Controller1クラスの一部のみ）
3. **BE-1／BE-4**: Service層・レスポンスDTOが無く、Controllerに責務が集中している
4. **FE-2／FE-7〜FE-10、BE-3／BE-8〜BE-10**: 個別の小さな改善余地（優先度は低め）

次回チェック時は、この「今後の課題」のうちどれが対応済みになったかを、新しい日付の見出しで追記すること。
