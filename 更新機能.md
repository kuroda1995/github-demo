# 更新機能：カードの編集

作成日: 2026-08-06
関連ドキュメント: [要件定義書](./要件定義書.md)（3-2. カードの編集）／[設計書](./設計書.md)

フェーズ2（バックエンド・DB化）時点での、カード更新機能の実装内容をまとめる。タイトル・説明文・優先度・期限の編集に加えて、**ステータス（所属する列）の変更と、変更時の自動並び替え**（[Issue #11](https://github.com/kuroda1995/github-demo/issues/11) / [PR #12](https://github.com/kuroda1995/github-demo/pull/12)）を含む。

## 1. できること

- カードをクリックすると編集フォームが開き、以下をまとめて編集できる
  - タイトル（必須）
  - 説明文（任意）
  - 優先度（高／中／低）
  - 期限（任意）
  - **ステータス（未着手／進行中／完了のどの列に属するか）**
- ステータスを変更して保存すると、カードが選択した列に移動し、**移動先の列の中で優先度順（高→中→低）に自動的に並び替えられる**
- 「保存」ボタンで確定、「キャンセル」ボタンまたはEscキーで編集を取り消す

## 2. 画面操作の流れ

1. 編集したいカードをクリックする
2. `CardForm`が編集モードで開く（現在の値が初期表示される）
3. 内容を書き換える。ステータスを変えたい場合は「ステータス」のプルダウンから列を選び直す
4. 「保存」ボタンを押す（タイトル欄でのEnterキーでも保存できる）
5. 書き換えた内容でカードの表示が更新される。ステータスを変えた場合は、選んだ列の中に、優先度順の正しい位置に挿入された状態で表示される

### カードの移動方法はもう一つある

ドラッグ＆ドロップで別の列にカードを移動することもできる（[Board.jsx](./frontend/src/components/Board.jsx)の`handleDragEnd`）。この場合は自動並び替えは行われず、ドロップした位置にそのまま挿入される。編集フォームからステータスを選んだ場合との違いはこの並び替えの有無のみ。

## 3. 関連ファイル（フロントエンド）

| ファイル | 役割 |
|----------|------|
| [Card.jsx](./frontend/src/components/Card.jsx) | カードクリックで編集モード（`isEditing`）に切り替え、`CardForm`を表示する |
| [CardForm.jsx](./frontend/src/components/CardForm.jsx) | タイトル・説明文・優先度・期限・ステータスの編集フォーム本体。ステータスの`<select>`（`.cf-status`）は`columns`propが渡された場合のみ表示される |
| [Column.jsx](./frontend/src/components/Column.jsx) / [Board.jsx](./frontend/src/components/Board.jsx) | 全列の一覧（`columns`）をカードまで橋渡しし、ステータス選択肢として使えるようにする |
| [useCards.js](./frontend/src/hooks/useCards.js) | `updateCard(id, values)`：フィールドの更新に加え、`values.columnId`が現在の列と異なる場合は列の移動＋優先度順の自動並び替えを行う |
| [cardUtils.js](./frontend/src/utils/cardUtils.js) | `moveCard`（列の移動と`order`の振り直し）、`sortColumnCards`（優先度順・期限順への並び替え）の実処理 |

### 自動並び替えの仕組み

`useCards.js`の`updateCard`は、ステータス（`columnId`）が変わったことを検知すると、以下の順で処理する。

1. `moveCardInList`でカードを移動先の列の末尾に挿入する
2. `sortColumnCards(nextCards, nextColumnId, "priority")`で、移動先の列を優先度順（高→中→低）に並び替え、`order`を振り直す
3. 並び替えの結果、`order`または`columnId`が変わったカードだけをバックエンドにPUTで保存する（移動先の列に元々あった他のカードの`order`も一緒に更新されることがある）

既存の「優先度順」並び替えボタン（[SortControls.jsx](./frontend/src/components/SortControls.jsx)）と同じ並び替えロジック（`sortColumnCards`）を再利用している。

## 4. 関連ファイル（バックエンド）

| ファイル | 役割 |
|----------|------|
| [CardController.java](./backend/src/main/java/com/example/trello/controller/CardController.java) | `PUT /api/cards/{id}`（`updateCard`）：指定IDのカードを取得し、リクエスト内容で上書き保存する |

バックエンド側にはステータス変更専用のAPIは無く、通常の更新API（`PUT /api/cards/{id}`）の`columnId`を書き換えることで列の移動を表現している。優先度順の並び替え自体はフロントエンド側で計算し、影響を受けた各カードを個別にPUTすることでDBに反映する。

## 5. API仕様

| メソッド | パス | リクエストボディ | レスポンス |
|----------|------|-------------------|------------|
| PUT | /api/cards/{id} | `{ title, description, priority, dueDate, columnId, order }` | 200 OK + 更新後のカード |

- 存在しないIDを指定した場合は404エラー
- `columnId`が存在しない列を指している場合は400エラー
- ステータス変更時は、移動先の列で影響を受けた複数のカードについて、この`PUT`が複数回（並列に）呼ばれる
