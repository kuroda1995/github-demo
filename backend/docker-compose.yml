services:
  postgres:
    image: postgres:16
    container_name: trello-postgres
    environment:
      POSTGRES_DB: trello
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - trello-pgdata:/var/lib/postgresql/data

volumes:
  trello-pgdata:
