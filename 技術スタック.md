# 技術スタック

このプロジェクトで使用している技術要素の一覧。各技術を選んだ理由は [要件定義書](./要件定義書.md)・[設計書](./設計書.md) を参照。

## フロントエンド（`frontend/`）

| 区分 | 技術 | バージョン | 用途 |
|------|------|-----------|------|
| 言語 | JavaScript (JSX) | - | UIロジックの実装 |
| UIライブラリ | [React](https://react.dev/) | ^19.2.8 | コンポーネントベースの画面構築 |
| ビルドツール | [Vite](https://vitejs.dev/) | ^8.2.0 | 開発サーバー・バンドル |
| Reactプラグイン | @vitejs/plugin-react | ^6.0.4 | ViteでのReact（JSX/Fast Refresh）サポート |
| ドラッグ＆ドロップ | [@dnd-kit/core](https://dndkit.com/) / `@dnd-kit/sortable` / `@dnd-kit/utilities` | ^6.3.1 / ^10.0.0 / ^3.2.2 | カードのドラッグ＆ドロップによる列間移動 |
| Lint | [oxlint](https://oxc.rs/docs/guide/usage/linter.html) | ^1.75.0 | 静的解析（コード品質チェック） |
| 型情報 | @types/react / @types/react-dom | ^19.2.17 / ^19.2.3 | エディタ補完用の型定義（TypeScript自体は未導入） |
| データ永続化（フェーズ1） | localStorage | ブラウザ標準 | ブラウザ内でのカードデータ保存 |
| データ永続化（フェーズ2） | REST API（`src/api/`） | - | バックエンドAPI経由でのカードデータ保存 |

## バックエンド（`backend/`）

| 区分 | 技術 | バージョン | 用途 |
|------|------|-----------|------|
| 言語 | Java | 21（JDK） | バックエンドロジックの実装 |
| フレームワーク | [Spring Boot](https://spring.io/projects/spring-boot) | 4.1.0 | REST APIサーバーの構築 |
| ビルドツール | [Gradle](https://gradle.org/)（Gradle Wrapper同梱） | - | 依存関係管理・ビルド |
| Web | Spring Boot Starter Web MVC | - | RESTエンドポイントの実装 |
| ORM | Spring Data JPA（Hibernate） | - | JavaクラスとDBテーブルのマッピング |
| バリデーション | Spring Boot Starter Validation | - | リクエストの入力チェック |
| DBドライバ | PostgreSQL JDBC Driver | - | PostgreSQLへの接続 |
| テスト | Spring Boot Starter Test（JPA / Validation / Web MVC）、JUnit Platform | - | 単体・統合テスト |
| CORS設定 | `config/CorsConfig.java` | - | `http://localhost:5173`（フロントエンド）からのアクセスを許可 |

### バックエンドの構成

```
backend/src/main/java/com/example/trello/
├── TrelloBackendApplication.java   # エントリーポイント
├── config/CorsConfig.java          # CORS設定
├── controller/                     # REST APIエンドポイント
│   ├── CardController.java
│   └── ColumnController.java
├── dto/CardRequest.java            # リクエストボディの受け取り用
├── entity/                         # DBテーブルに対応するクラス
│   ├── BoardColumn.java
│   ├── Card.java
│   └── Priority.java                # 優先度（高/中/低）の列挙型
└── repository/                     # DBアクセス（Spring Data JPA）
    ├── CardRepository.java
    └── ColumnRepository.java
```

## データベース

| 区分 | 技術 | バージョン | 用途 |
|------|------|-----------|------|
| DBMS | [PostgreSQL](https://www.postgresql.org/) | 16 | カード・列データの永続化 |
| 起動方法 | Docker Compose（`backend/docker-compose.yml`） | - | ローカルにPostgreSQLコンテナを構築 |
| スキーマ管理 | Hibernate `ddl-auto=update`（学習用途のみ） | - | エンティティ定義からテーブルを自動生成・更新 |
| 初期データ | `backend/src/main/resources/data.sql` | - | 列（todo / doing / done）の初期投入 |

### テーブル構成（概要）

ER図の詳細は [設計書](./設計書.md#1-er図データの実体と関連) を参照。

| テーブル | 内容 |
|----------|------|
| `board_columns` | 列（todo / doing / done）。3件固定 |
| `cards` | カード（タイトル・説明文・優先度・期限・所属列・並び順） |

## 開発ツール・環境

| 区分 | 技術 | 用途 |
|------|------|------|
| バージョン管理 | Git / GitHub | ソースコード管理（運用ルールは [CLAUDE.md](./CLAUDE.md) 参照） |
| コンテナ | Docker Desktop | PostgreSQLのローカル起動 |
| JDK管理 | [Eclipse Temurin](https://adoptium.net/) 等 | JDK 21の導入 |
| AI開発支援 | Claude Code | 実装・ドキュメント作成支援（`.claude/skills/` にプロジェクト固有スキルを定義） |
| 対応ブラウザ | Google Chrome（最新版） | 開発・動作確認対象（非機能要件） |

## API通信

| 項目 | 内容 |
|------|------|
| 通信方式 | REST API（JSON） |
| フロントエンドのAPIベースURL | `frontend/.env` の `VITE_API_BASE_URL`（デフォルト: `http://localhost:8080`） |
| ポート | バックエンド: `8080`固定 / フロントエンド: `5173`固定（CORS設定と対応しているため変更不可、詳細は [CLAUDE.md](./CLAUDE.md#5-開発サーバー起動時のポート管理) 参照） |
| エンドポイント一覧 | [設計書](./設計書.md#3-api設計概要) 参照 |

## 開発フェーズと技術の対応

| フェーズ | 内容 | 使用技術 |
|----------|------|----------|
| フェーズ1（完了） | フロントエンドのみでの実装、データはブラウザ内保存 | React, Vite, @dnd-kit, localStorage |
| フェーズ2（進行中） | データ保存先をバックエンド＋DBに置き換え | 上記フロントエンド技術 + Java, Spring Boot, Gradle, PostgreSQL, Spring Data JPA |
